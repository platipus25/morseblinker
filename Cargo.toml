[package]
name = "morseblinker"
version = "0.1.0"
edition = "2024"

[dependencies]
hound = { version = "3.5.1", optional = true }
tokio = { version = "1.52.3", features = ["full"] }

[features]
wav = ["dep:hound"]
